# client/

Browser-only modules: audio priming + playback, smooth tick interpolation,
and other code that can't run on the server. UI components live under `app/`
(Next.js App Router); this folder is for non-component browser utilities.
